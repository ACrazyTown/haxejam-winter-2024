package props.fish;

enum abstract FishLocation(Int) from Int to Int
{
    var FINDFISH_LAKE = 0;
    var UNDERLAKE = 1;
    var SALMON_SEA = 2;
    var CATFISH_CANAL = 3;
    // ;)
    var HAKS_HARBOR = 4;
    var FLIKSEL_FJORD = 5;
}
