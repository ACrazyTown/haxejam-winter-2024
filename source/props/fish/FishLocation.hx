package props.fish;

enum abstract FishLocation(Int) from Int to Int
{
    var FINDFISH_LAKE = 0;
    var UNDERLAKE = 1;
    var SALMON_SEA = 2;
    var CATFISH_CANAL = 3;
    var NUCLEAR_WASTELAND = 4;
    // ;)
    var HAKS_HARBOR = 5;
    var FLIKSEL_FJORD = 6;
}
